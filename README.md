# Atom TKS Syntax Highlighting

Atom syntax highlighting for TKS files

## Install

Install the package `language-tks` in Atom (Preferences->Packages) or Atom's package manager from a shell:

```bash
$ apm install language-tks
```

## Syntax support

TODO
